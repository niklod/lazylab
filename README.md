# LazyLab

A Terminal UI for interacting with GitLab, inspired by [gh-lazy](https://github.com/gizmo385/gh-lazy).

## Install

```bash
uv sync
```

## Usage

```bash
lazylab
```

## Configuration

Edit `~/.config/lazylab/config.yaml` with your GitLab URL and token.
